name: ci

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/setup-uv@v5
      - name: Set up Python
        run: uv python install 3.11
      - name: Sync dependencies
        run: uv sync --frozen --extra dev
      - name: Smoke-check CLI
        run: uv run kb-agent --help
      - name: Run tests
        run: uv run --with pytest python -m pytest
      - name: Run demo path
        run: uv run kb-agent --query-fixture fixtures/queries/m0_query.json --vault-root vault
      - name: Run eval path
        run: uv run kb-eval --eval-fixture fixtures/evals/cases.json --vault-root vault
