# Шаблон передачи работы

Используйте этот файл как короткую инженерную заметку к сдаче или к вопросу о
помощи. Достаточно одного экрана текста.

## Что я проверял

- Вопрос или сценарий:
- `run_id`:
- Eval run id:

## Какие команды прошли

```bash
uv sync --frozen --extra dev
uv run pytest
uv run kb-agent --query-fixture fixtures/queries/project_query.json --vault-root vault
uv run kb-eval --eval-fixture fixtures/evals/cases.json --vault-root vault
```

## Куда смотреть

- Сводка запуска:
- Ответ:
- Source-map:
- Пакет контекста:
- Трейс:
- Отчёт проверки состояния:
- Eval-отчёт:

## Что получилось

1. Какой контекст выбрал харнес:
2. На какие источники опирается ответ:
3. Что показала проверка состояния:
4. Что показали evals:

## Границы доверия

1. Какие входные данные в этом запуске считаются недоверенными:
2. Какие тулы только читают данные:
3. Какие тулы могут писать и почему модель не получает их как `model_safe: true`:

## Как проходил маршрут

- Сколько времени занял первый успешный запуск:
- Что первым сбило с толку:
- Где понадобилась помощь:
- Насколько уверенно я могу объяснить `run_id` и артефакты запуска по шкале 1-5:

## Что осталось вне объёма

- 

## Где я сомневаюсь

- 
