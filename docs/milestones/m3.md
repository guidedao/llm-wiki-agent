# M3

В текущем `main` майлстоун `M3` уже реализован как учебный проверочный слой
управляемого запуска. Это не ветка, не тег и не задание «дописать
runtime с нуля», а карта чтения: студент берёт один `run_id` и связывает
запись запуска, трейс, health-отчёт, бюджет, tools и eval.

## Цель

В майлстоуне `M3` мы проверяем учебный слой управляемого выполнения поверх уже
готового пути `M2`. В текущем учебном репозитории этот слой уже реализован;
задача студента — прочитать один запуск по артефактам, а не дописать большой
production runtime с нуля.

Система должна показывать не только ответ, план и пакет контекста, но и то, как
конкретный запуск жил как объект: где он стартовал, чем завершился, какие
события оставил и проходит ли базовую проверку состояния.

## Связь с курсом

- **Мониторинг и оценка**
- **Обвязка**
- **Рабочая среда**

## Стартовое состояние

- точка входа: текущий `main` после прохождения `M2`
- до начала уже должно работать:
  - `uv run pytest`
  - `uv run kb-agent --query-fixture fixtures/queries/m2_query.json --vault-root vault`
  - `artifacts/plans/<run_id>.json`
  - `artifacts/context/<run_id>.json`
  - `artifacts/traces/<run_id>.jsonl`

## Объём

Нужно уметь проверить, что в текущем проекте есть:

- запись запуска как состояние запуска, а не случайный JSON;
- `terminal_reason` при успешном завершении;
- реестр контрактов тулов для текущих режимов чтения и записи;
- отчёт проверки состояния для ключевых артефактов запуска;
- `run_budget.yaml` как читаемый бюджет по стадиям;
- понятная диагностика запуска по трейсам, артефактам и отчёту проверки
  состояния.

Вне объёма:

- распределённая очередь
- настоящие фоновые worker-процессы
- production leases
- полноценный pause/resume/cancel runtime
- обратная запись в wiki
- UI подтверждения
- большой eval-каркас с LLM-judge или внешними сервисами

## Команды

- `uv run pytest`
- `uv run kb-agent --query-fixture fixtures/queries/m2_query.json --vault-root vault`
- `uv run kb-eval --eval-fixture fixtures/evals/cases.json --vault-root vault`

## Ожидаемые артефакты

- `artifacts/runs/<run_id>.json`
- `artifacts/plans/<run_id>.json`
- `artifacts/context/<run_id>.json`
- `artifacts/tools/<run_id>.json`
- `artifacts/traces/<run_id>.jsonl`
- `artifacts/health/<run_id>.json`
- `artifacts/evals/<eval_run_id>.json`
- `run_budget.yaml`
- `docs/trust-boundaries.md`
- `docs/tool-contracts.md`
- `vault/outputs/<run_id>.md`
- `vault/outputs/<run_id>-summary.md`

## Критерии приёмки

- [ ] `uv run pytest` проходит
- [ ] `uv run kb-agent --query-fixture fixtures/queries/m2_query.json --vault-root vault` проходит
- [ ] запись запуска содержит `status: completed`
- [ ] запись запуска содержит `terminal_reason: success`
- [ ] отчёт проверки состояния создан и содержит `status: pass`
- [ ] eval-отчёт создан и содержит `status: pass`
- [ ] трейс содержит события планирования и ответа
- [ ] сводка запуска создана и ссылается на ответ, контекст, тулзы, трейс и отчёт проверки состояния
- [ ] реестр тулов отделяет режим только для чтения от режима записи
- [ ] `run_budget.yaml` есть и описывает основные стадии запуска
- [ ] live-путь, если он использовался, пишет только ответ и метаданные
      как артефакты
      и не делает обратную запись в `vault/raw/` или `vault/wiki/`

## Подсказки для отладки

- если отчёт проверки состояния не прошёл, начните с отсутствующих проверок внутри
  `artifacts/health/<run_id>.json`;
- если запуск не завершён, сравните `artifacts/runs/<run_id>.json` и трейс;
- если трейс неполная, проверьте, на каком событии остановился запуск;
- если budget выглядит как приложение про pricing, рамка стала слишком широкой: здесь нужен
  бюджет запуска по стадиям.

## Решение

- решение фиксируется текущим состоянием `main` и артефактами запуска

## Дальше

- Расширение A: контролируемая обратная запись
- Расширение B: большой eval-каркас и регрессионные отчёты
